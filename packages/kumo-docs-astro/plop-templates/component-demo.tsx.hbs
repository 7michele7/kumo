import { {{pascalCase name}} } from "@cloudflare/kumo";

/**
 * Basic demo — shown at the top of the component page as the hero example.
 * Keep this self-contained and representative of the most common use case.
 */
export function {{pascalCase name}}BasicDemo() {
  return (
    <{{pascalCase name}}>
      {/* TODO: render the component */}
    </{{pascalCase name}}>
  );
}

// TODO: Add more named exports below — one per example section in the MDX page.
// Rules:
//   - Each function MUST end with the "Demo" suffix (e.g. {{pascalCase name}}VariantsDemo)
//   - Keep demos self-contained; import everything they need from "@cloudflare/kumo"
//   - Add a JSDoc comment to any demo that needs a description in the registry
//
// Example:
//
// export function {{pascalCase name}}VariantsDemo() {
//   return (
//     <div className="flex flex-wrap items-center gap-2">
//       <{{pascalCase name}} variant="primary">Primary</{{pascalCase name}}>
//       <{{pascalCase name}} variant="secondary">Secondary</{{pascalCase name}}>
//     </div>
//   );
// }
